# 📌 기능 설계 문서

📌 운영 원칙
PR 태그에 체크한 항목만 details 열고 나머지는 삭제
리뷰 시 필요한 부분만 펼쳐서 확인 가능


# 🧠 Feature Design & Operation Specification — <Login Page>
> 로그인 페이지 (Login.tsx)

---

## 0️⃣ 문서 통제 정보 (Document Control)

### 문서 기본 정보
- 문서 ID: GEN-009
- 기능명: 사용자 로그인 (User Authentication)
- 시스템명: SafeGuard Frontend
- 작성자: AI Agent
- 검토자: Tech Lead
- 승인자: PM

### 문서 이력 관리
| 버전 | 날짜 | 변경 요약 | 작성자 |
|----|----|---------|------|
| 1.0 | 2026-01-17 | 최초 작성 | AI Agent |

### 문서 상태
- [ ] Draft
- [x] In Review
- [ ] Approved
- [ ] Deprecated

---

## 1️⃣ 비즈니스 컨텍스트 (Business Context)

### 1.1 비즈니스 목적
- 사용자 식별을 통해 개인화된 서비스(내 민원 관리, 댓글 작성 등) 및 관리자 기능 접근 권한 부여.

---

## 3️⃣ 요구사항 정의 (Requirements)

### 3.1 기능 요구사항 (Functional Requirements)

| ID | 요구사항 | 우선순위 |
|---|--------|--------|
| FR-1 | 아이디와 비밀번호를 입력받아 인증을 수행해야 한다. | Must |
| FR-2 | 로그인 성공 시 사용자의 역할(User/Agency)에 따라 적절한 페이지로 리다이렉트해야 한다. | Must |
| FR-3 | 로그인 실패 시 명확한 에러 메시지를 표시해야 한다. | Must |
| FR-4 | 아이디 찾기, 비밀번호 재설정, 회원가입 페이지로의 링크를 제공해야 한다. | Should |

---

## 4️⃣ 기능 책임 및 경계 (Responsibility & Boundary)

### 4.1 책임 범위
- 사용자 크리덴셜 입력 수집, 인증 API 호출, 토큰 저장(로직 내재화 또는 쿠키 처리), 리다이렉트.

---

## 5️⃣ 시스템 아키텍처 상세 (Architecture)

### 5.1 논리 아키텍처
- `Login.tsx` -> `authAPI.login`
- Redirect Strategy:
  - Role `AGENCY` -> `/admin/dashboard`
  - Role `USER` -> `/` (Home)

### 5.4 기술 스택 (Technology Stack)
- React, Axios
- JWT (JSON Web Token) - 백엔드와 연동

---

## 6️⃣ End-to-End 처리 흐름 (E2E Flow)

```text
[User Input Credentials]
  ↓
[Click Submit]
  ↓
[POST /api/auth/login]
  ↓
[Success Response] -> [Check Role]
  ├─(Agency) -> Navigate to /admin/dashboard
  └─(User)   -> Navigate to /
```

---

## 7️⃣ 상세 설계 (Detailed Design)

### 7.2 핵심 비즈니스 로직 및 산출 공식 (Core Business Logic & Formulas)
- **입력 폼 상태 관리**: `userId`, `password` state 업데이트.
- **포커스 효과**: `onFocus`, `onBlur`를 통한 UX 인터랙션 처리 (테두리 색상 변경).

### 7.4 인터페이스 및 API 설계 (Interface & API Design)
- **POST `/api/auth/login`**
  - Input: `{ userId, password }`
  - Output: `{ token: string, user: { role, agencyName, ... } }`

---

## 8️⃣ 예외·오류 처리 설계 (Exception & Error Handling)

### 8.1 오류 분류
- **인증 실패**: "아이디 또는 비밀번호가 일치하지 않습니다."
- **서버 오류**: "서버와의 통신에 실패했습니다."

---

## 1️⃣7️⃣ 문서 연계 (Documentation Linkage)
- 관련 파일: `Login.tsx`, `Register.tsx`, `FindAccount.tsx`
