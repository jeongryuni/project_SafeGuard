# 📌 기능 설계 문서

📌 운영 원칙
PR 태그에 체크한 항목만 details 열고 나머지는 삭제
리뷰 시 필요한 부분만 펼쳐서 확인 가능


# 🧠 Feature Design & Operation Specification — <List Page>
> 민원 목록 페이지 (List.tsx)

---

## 0️⃣ 문서 통제 정보 (Document Control)

### 문서 기본 정보
- 문서 ID: GEN-008
- 기능명: 민원 목록 조회 및 검색 (Complaint List & Search)
- 시스템명: SafeGuard Frontend
- 작성자: AI Agent
- 검토자: Tech Lead
- 승인자: PM

### 문서 이력 관리
| 버전 | 날짜 | 변경 요약 | 작성자 |
|----|----|---------|------|
| 1.0 | 2026-01-17 | 최초 작성 | AI Agent |

### 문서 상태
- [ ] Draft
- [x] In Review
- [ ] Approved
- [ ] Deprecated

---

## 1️⃣ 비즈니스 컨텍스트 (Business Context)

### 1.1 비즈니스 목적
- 등록된 모든 민원을 검색하고 필터링하여 원하는 정보를 빠르게 찾을 수 있도록 지원.
- 담당 기관(Agency)은 자신에게 배정된 민원을 관리하거나 엑셀로 다운로드하여 업무 효율성 증대.

---

## 3️⃣ 요구사항 정의 (Requirements)

### 3.1 기능 요구사항 (Functional Requirements)

| ID | 요구사항 | 우선순위 |
|---|--------|--------|
| FR-1 | 페이지네이션(Pagination)을 지원하여 대량의 데이터를 효율적으로 표시해야 한다. | Must |
| FR-2 | 제목 및 내용 키워드 검색, 카테고리/지역/상태(처리중 등) 필터링을 제공해야 한다. | Must |
| FR-3 | 정렬 옵션(최신순, 과거순, 좋아요순)을 제공해야 한다. | Should |
| FR-4 | 기관 관리자(Agency)는 '엑셀 다운로드' 및 '내 담당 민원만 보기' 기능을 사용할 수 있어야 한다. | Must |
| FR-5 | 민원 리스트에서 바로 상세 페이지로 이동하거나, 권한이 있는 경우 삭제할 수 있어야 한다. | Must |

---

## 4️⃣ 기능 책임 및 경계 (Responsibility & Boundary)

### 4.1 책임 범위
- 복합 쿼리 파라미터 관리(URL Sync), 데이터 목록 렌더링, 엑셀 익스포트 클라이언트 처리.

---

## 5️⃣ 시스템 아키텍처 상세 (Architecture)

### 5.1 논리 아키텍처
- `List.tsx` -> `complaintsAPI.getList`
- State Management: `URLSearchParams` (URL 기반 상태 관리로 새로고침 시에도 필터 유지)

### 5.4 기술 스택 (Technology Stack)
- React Hooks (`useState`, `useEffect`, `useSearchParams`)
- Browser API (`Blob`, `URL.createObjectURL` for Excel download)

---

## 7️⃣ 상세 설계 (Detailed Design)

### 7.2 핵심 비즈니스 로직 및 산출 공식 (Core Business Logic & Formulas)
- **페이지네이션 그룹 계산**:
  ```javascript
  const groupSize = 5;
  const currentGroup = Math.ceil(page / groupSize);
  // 현재 페이지가 속한 1~5, 6~10 등의 그룹 범위를 계산하여 버튼 렌더링
  ```
- **CSV 생성**: 데이터를 쉼표(`,`)로 구분된 문자열로 변환하고 `\uFEFF` (BOM)을 추가하여 한글 깨짐 방지.

### 7.4 인터페이스 및 API 설계 (Interface & API Design)
- **GET `/api/complaints`**
  - Query Params: `page`, `limit`, `search`, `category`, `status`, `region`, `sort`, `order`, `agencyNo`, `myAgencyOnly`

---

## 8️⃣ 예외·오류 처리 설계 (Exception & Error Handling)

### 8.1 오류 분류
- **검색 결과 없음**: "등록된 민원이 없습니다" 메시지 및 첫 민원 등록 버튼 노출.
- **삭제 실패**: 권한 없음 또는 서버 오류 시 `alert` 표시.

---

## 1️⃣7️⃣ 문서 연계 (Documentation Linkage)
- 관련 파일: `List.tsx`, `Detail.tsx`
